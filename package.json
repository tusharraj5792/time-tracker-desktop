{
  "name": "timetracker",
  "productName": "TimeTracker",
  "version": "1.0.1",
  "description": "TimeTracker", 
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "buildx86": "electron-packager . --platform=win32 --arch=ia32  TimeTracker --icon=assets/img/small-logo.ico --out=build/x86 --f",
    "buildx64": "electron-packager . --platform=win32 --arch=x64  TimeTracker --icon=assets/img/small-logo.ico --out=build/x64 --f"
  },
  "devDependencies": {
    "electron": "^15.3.7",
    "electron-installer-dmg": "^3.0.0",
    "electron-wix-msi": "^3.2.0"
  },
  "dependencies": {
    "active-win": "^8.1.0",
    "electron-packager": "^16.0.0",
    "is-online": "^9.0.1"
  }
}
